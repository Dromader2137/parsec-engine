[package]
name = "parsec-engine-examples"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25.9"
parsec-engine = { path = "../parsec-engine" }
parsec-engine-ecs = { path = "../parsec-engine-ecs" }
parsec-engine-error = { path = "../parsec-engine-error" }
rand = "0.10.0"
